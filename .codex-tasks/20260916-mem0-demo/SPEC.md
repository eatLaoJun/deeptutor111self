# Mem0 standalone demo

Create an isolated learning demo, not a production integration. L1/L2/L3 are interview designs, not implemented business capabilities. Pin mem0ai; provide local persistence, explicit credentials, CRUD CLI, offline tests and Chinese setup instructions. Never read existing project secrets or submit real student data.

Acceptance: offline tests pass; pinned SDK configuration/API checked; live provider verification reported separately if credentials are unavailable.
